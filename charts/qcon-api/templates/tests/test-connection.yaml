apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qcon-api.fullname" . }}-test-connection"
  labels:
    {{- include "qcon-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qcon-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
